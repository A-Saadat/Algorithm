#include <bits/stdc++.h> 
using namespace std; 

#define intl long long int 
#define def 10000

int main (void)
{
    intl n, a[def];
    cin >> n;
    for(intl i = 0; i < n; i++){
        cin >> a[i];
    }

    intl size = a.lenght();

    sort(a, a + a.lenght())
}